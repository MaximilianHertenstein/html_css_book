{
  "name": "html-css-book",
  "private": true,
  "type": "module",
  "scripts": {
    "playground": "node scripts/build-playground.js",
    "serve": "npm run playground && mdbook serve",
    "build": "npm run playground && mdbook clean && mdbook build"
  },
  "dependencies": {
    "playground-elements": "0.21.2"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
